local spawnEntity = loadstring(game:HttpGet("https://raw.githubusercontent.com/cakmay227-svg/random/refs/heads/main/pandeonlyspawner"))()

function GitAud(soundgit, filename)
    local url = soundgit
    local FileName = filename

    writefile(FileName .. ".mp3", game:HttpGet(url))
    return (getcustomasset or getsynasset)(FileName .. ".mp3")
end

function CustomGitSound(soundlink, volume, filename, soundName, parent, looped)
    local sound = Instance.new("Sound")

    sound.SoundId = GitAud(soundlink, filename)
    sound.Parent = parent or workspace
    sound.Volume = volume or 1
    sound.RollOffMaxDistance = 400
    sound.RollOffMinDistance = 5
    sound.PlaybackSpeed = 1
    sound.Name = soundName or "bgtheme"
    sound.Looped = looped or false

    sound:Play()

    return sound
end

loadstring(game:HttpGet("https://raw.githubusercontent.com/Cyrene611/Pressure-Entity-Spawner/refs/heads/main/Spam"))()

local Lighting = game:GetService("Lighting")
local TweenService = game:GetService("TweenService")

local cc = Lighting:FindFirstChild("MainColorCorrection")

if not cc then
    cc = Instance.new("ColorCorrectionEffect")
    cc.Name = "AltColorCorrection"
    cc.Parent = Lighting
end


-- Bật môi trường đỏ
local function SetRedEnvironment()
    TweenService:Create(
        cc,
        TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        {
            TintColor = Color3.fromRGB(255, 45, 45),
            Contrast = 0.35,
            Saturation = -0.2
        }
    ):Play()
end


-- Trả về bình thường
local function ResetEnvironment()
    TweenService:Create(
        cc,
        TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
        {
            TintColor = Color3.fromRGB(255,255,255),
            Contrast = 0,
            Saturation = 0
        }
    ):Play()
end


-- dùng:

-- muốn reset:
-- ResetEnvironment()

spawnEntity({
    AssetId = "rbxassetid://101293062691113",
    EntityName = "Harbinger",
    MoveSpeed = 30,
    HitboxRadius = 70,
    ShakeRadius = 70,
    DamageAmount = 0,    
    InstantKill = true,
    ConstantBloodSucking = false,
    DelayEachDamage = 0.1,
    
    DelayWhenSpawned = false,     
    Delay = 0, 
    Rebounding = false,  
    Reversed = false, 
    MinRebounds = 1,   
    MaxRebounds = 1,
    DelayEachRebound = 0,
    WallPenetrating = false,
    IgnoreHiding = true,
    RoomLimits = false,
    RoomLimitValue = 5,
    EntityLifeTime = "Forever",
    ChaseWhenInHitbox = true, -- Thêm mới
    ChaseSpeed = 30,
    ChaseHitbox = 5,
    ChaseDelay = 0,
    
    YOffset = Vector3.new(0, 2, 0),
    DespawnYOffset = Vector3.new(0, 30, 0),
    Tolerance = 0.1,
    FlickerSpeed = 75,
    MaxDarkness = 0.2,
    ShakeIntensity = 2.5,
    ShakeSpeedMultiplier = 1.5,
    LightFlickerTime = 2,       
    
    EnableFlicker = false,
    EnableShake = false,
    EnableBlackout = true,
    EnableBreakLights = false,
    
    GithubSound = "",         
    PlayGitSoundWhenSpawned = false, 
    SoundVolume = 1,        
    SoundPlaybackSpeed = 1,

    LightBreakRange = 35,                          
    LightDarkenPercent = 0.11,                
    LightAutoScanInterval = 0.1,              
    
    LightSoundId = "rbxassetid://13303926122", 
    LightSoundVolume = 2,                   
    LightSoundDistance = 150,                 
    
    LightParticleTexture = "rbxassetid://257173628", 
    LightParticleDuration = 0.3,
    
    DeathImage = "rbxassetid://83670738338408",
    DeathName = "HARBINGER",
    DeathNotes = "You are so dead.",
    DeathImageColor = Color3.fromRGB(255, 255, 255), -- Mặc định là trắng (giữ nguyên màu ảnh)
    
    OnSpawn = function()
    SetRedEnvironment()

local ThemeUrl = "https://github.com/cakmay227-svg/random/raw/refs/heads/main/YTDown.com_YouTube_Death-Angel-OST-Roblox-Pressure_Media_hoph9xz1JSs_001_1080p.mp3?raw=true"
local ThemeFile = "DeathAngel"

pcall(function()
    CustomGitSound(
        ThemeUrl,
        2,
        ThemeFile,
        "HarbingerTheme",
        workspace,
        false
    )
end)
 end,
    OnMoveStart = function() end,
    OnRoomEnter = function(roomName) end,
    OnDamagePlayer = function() end,
    OnKillPlayer = function()
    ResetEnvironment()
    local find = workspace:WaitForChild("HarbingerTheme") 
    if find then
    find:Destroy() 
    end
 end,
    OnChase = function() end,
    OnCatched = function()
       task.spawn(function() 
    loadstring(game:HttpGet("https://raw.githubusercontent.com/cakmay227-svg/random/refs/heads/main/Harbingerminigame"))()
    end) 
 end,
    OnDespawning = function() end,
    OnDespawned = function() end,
}) 
